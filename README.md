# ltailwindcss

```
npx @tailwindcss/cli -i ./src/input.css -o ./public/output.css --watch
npm install live-server -g
live-server ./public
```

- `@apply` directive is used to avoid the repeat of the classes for each card.
